import {ShippingContainer} from './container';
import {Ship} from './ship';
import {Transporter} from './transporter';

function findContainersByDestination(containers:ShippingContainer[], desination:string) {

    let result = [];
}

function findOverweightTransporters(transporters:Transporter[]) {

    let result = [];

    result.filter(index => this.transporters.isOverweight = true)
}

function isSafeToAddContainer(ship:Ship, container:ShippingContainer) {

    if (this.ship.isOverweight = false) {
        return true;
    } else {
        return false;
    }
}